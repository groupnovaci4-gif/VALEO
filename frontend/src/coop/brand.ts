// VALEO — identité visuelle.
//
// SOURCE UNIQUE du logo : tous les écrans passent par ici. Remplacer le
// fichier `assets/images/valeo-logo.png` suffit donc à mettre à jour l'appli
// partout — il n'y a pas un `require` de logo dispersé dans chaque écran.
//
// Les icônes système (icône d'application, écran de démarrage, favicon) sont
// déclarées séparément dans `app.json` : elles sont lues par Expo au moment du
// build, pas par le code. Voir `assets/images/README.md` pour la liste exacte
// des fichiers à fournir et leurs contraintes.

/** Logo officiel VALEO, complet (emblème + nom + signature). */
export const VALEO_LOGO = require("../../assets/images/valeo-logo.png");
